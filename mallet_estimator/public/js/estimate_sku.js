// Estimate SKU: no custom buttons. Attach the OpenCutList Estimate PDF (and
// Parts CSV) in the Material section and Save — material + operation quantities
// import automatically. The labor table auto-fills with the standard phases and
// their workstations on the first save; the computed qty cells (steps 1-4) are
// locked via read_only_depends_on on the child field.
